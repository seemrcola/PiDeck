# pi-desktop v0.4.9

## Highlights
- Added a project session history modal with inline session renaming.
- Added terminal right-click copy for selected text, with a lightweight copied hint.
- Fixed Codex-imported sessions by writing compatible assistant usage metadata so imported conversations can continue safely.
- Changed Codex session import defaults so no sessions are selected automatically.
- Refined historical session rows with a compact Codex-style layout and lighter rename controls.

## Verification
- `npm run typecheck`
- `npm run build`
